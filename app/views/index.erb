<!DOCTYPE html>
<html>
  <head>

  <link rel="stylesheet" type="text/css" href="css/styles.css">

    <title>Fwitter (any similarities to Twitter are purely coincidental - please do not sue)</title>
  </head>
  
  <body>
    <div>
      <div class="container">
        <div id="top">
            <form id="new_fweet" action="/" method="POST">
              <input class="input" type="text" name="fweet" placeholder="  fweet">
              <button id="submit" class="btn btn-default" type="submit"><img id="twitter_icon"src="https://g.twimg.com/Twitter_logo_blue.png"></button>
            </form>
          </div>
      </div>
        <div class="container" id="fweet_container">
          <ul>
            <% @fweets = @fweets.reverse %>
              <% @fweets.each do |fweet| %>
                <li class="fweet">
                  <p class="username"><%= "@#{fweet.user.username}"%></p>
                  <p class="content"><%= "#{fweet.content}" %></p>
                </li>
              <% end %>
          </ul>
      </div>
    </div>
  </body>
</html>